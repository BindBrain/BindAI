[project]
name = "bindai-config"
version = "0.2.1"
description = "BindAI Configuration"
requires-python = ">=3.11"

dependencies = [
    "PyYAML",
    "bindai-group>=0.2.0",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]