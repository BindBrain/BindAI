[project]
name = "bindai-tool"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "bindai-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bindai_tool"]