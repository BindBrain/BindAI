[build-system]
requires = [
    "setuptools>=68",
    "wheel"
]
build-backend = "setuptools.build_meta"


[project]
name = "bindai-mcp"
version = "0.1.0"
description = "MCP integration package for BindAI"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "httpx",
    "bindai-tool",
]

[tool.setuptools.packages.find]
where = [
    "src"
]