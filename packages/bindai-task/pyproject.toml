[project]
name = "bindai-task"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "bindai-agent",
    "bindai-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bindai_task"]