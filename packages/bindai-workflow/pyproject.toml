[project]
name = "bindai-workflow"
version = "0.2.0"
requires-python = ">=3.12"
readme = "README.md"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bindai_workflow"]