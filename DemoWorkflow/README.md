# Workflow Basic

## Setup

```bash
cp .env.example .env
```

Edit `.env` and add your OpenAI API key.

Install dependencies:

```bash
pip install -e ../../packages/*
```

Run:

```bash
python main.py
```