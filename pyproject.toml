[project]
name = "bindai"
version = "0.1.0"
description = "BindAI - Intent Driven AI Application Platform"
readme = "README.md"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false